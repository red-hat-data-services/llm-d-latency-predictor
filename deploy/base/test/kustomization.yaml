apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - functional-test-job.yaml
  # Uncomment to include stress/regression tests (requires high-resource cluster)
  # - stress-test-job.yaml
