# --- External Training Service (LoadBalancer for GKE) ---
apiVersion: v1
kind: Service
metadata:
  name: training-service-external
spec:
  type: LoadBalancer
  selector:
    app: training-server
    component: training
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8000
