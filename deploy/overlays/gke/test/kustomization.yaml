apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/test

patches:
  - path: node-selector-patch.yaml

images:
  - name: placeholder-for-test-image
    newName: ghcr.io/llm-d/llm-d-latency-predictor-test
    newTag: main
